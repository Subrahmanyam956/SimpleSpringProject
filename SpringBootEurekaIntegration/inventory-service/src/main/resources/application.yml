spring:
  application:
    name: INVENTORY-SERVICE

server:
  start: 8080

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: true
    fetch-registry: true
  serviceUrl:
    defaultZone: http://localhost:8761/eureka/

payment-url: http://PAYMENT-SERVICE/makePayment